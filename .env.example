# 禅道 MCP 认证。本文件只放占位符，会被提交。
# 真实值放本地 MCP 配置的 env，或环境变量，不要写回本文件。

# 必填：禅道地址（不含 /api.php/v1）
ZENTAO_URL=https://<禅道域名>/zentao

# 方式一（推荐）：账号 + 密码，自动换取 token 并在过期时刷新
ZENTAO_ACCOUNT=<禅道账号>
ZENTAO_PASSWORD=<禅道密码>

# 方式二（临时 / 测试）：现成 token，会过期
# ZENTAO_TOKEN=<token>
