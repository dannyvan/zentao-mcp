# zentao-mcp

不必 clone。配 `npx -y @dannyvan/zentao-mcp` 后，MCP `instructions` 会带上用法。

把 skill 装进本机 agent：`npx -y @dannyvan/zentao-mcp install-skill`（内部即 `npx skills add dannyvan/zentao-mcp -g`，会识别已装的 agent 并询问装哪几家）。
